first.insertAdjacentHtml("beforeend",'<div class="test">beforeend</div>')//this will insert just before the div take is closed
first.insertAdjacentHtml("beforebegin",'<div class="test">beforebegin</div>')//this will insert just before the div take is placed there
first.insertAdjacentHtml("aftrebegin",'<div class="test">afterbegin</div>')//this will insert the element just after the div tag is opened
first.insertAdjacentHtml("afterend",'<div class="test">afterend</div>')//this will insert the element just after it is closed 
//to remove a node we can use .remove() function to do this 
//if we do first.remove() the whole first is removed 
