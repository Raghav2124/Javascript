first.className="yellow text-black"
// classList it constains the list of all the classes we have 
//  we can remove a class from it by using first.classlist.remove("name of class which we want to remove ")
// to add a class we use first.classlist.add("to add the class which we want ")
// we can use toggle method if we want it can remove a class or add  a class like if we don't have a class it will add it and vice a versa
first.classList.add("red")
first.classList.remove("red")
first.classList.contains("red")
first.classList.toggle("red")